# Packages the orb-agent entrypoint pip-installs at container start
# (pointed to by INSTALL_WORKERS_PATH). Sources can be PyPI, a tar.gz, or a
# local folder containing pyproject.toml. During development, mount this repo
# and install from the folder:
./netbox-orb-extreme-platformone      # local folder with pyproject.toml
# orb-extreme-platformone==0.2.0      # ...or from a private index once published
# orb_extreme_platformone-0.2.0.tar.gz  # ...or from a built sdist in the mounted volume
