# Packages the orb-agent entrypoint pip-installs at container start
# (pointed to by INSTALL_WORKERS_PATH). Sources can be PyPI, a tar.gz, or a
# local folder containing pyproject.toml. Mount this repo at /opt/orb and
# install from the repo root:
.      # local folder with pyproject.toml (this repository)
# netbox-orb-extreme-platformone==0.2.0      # ...or from a private index once published
# netbox_orb_extreme_platformone-0.2.0.tar.gz  # ...or from a built sdist in the mounted volume
